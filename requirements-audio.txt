# Опциональные зависимости модуля «Музыка» для обработки MP3/WAV (аудио).
# MIDI (mido) уже включён в основной requirements.txt и работает везде.
# Установка: pip install -r requirements-audio.txt
# Без этих библиотек эндпоинты аудио возвращают понятную ошибку,
# а MIDI-функционал продолжает работать.
librosa>=0.10.0
soundfile>=0.12.0
