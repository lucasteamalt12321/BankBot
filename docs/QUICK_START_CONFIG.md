# Быстрый старт: Конфигурационные команды

## Введение

Конфигурационные команды позволяют управлять настройками бота без перезапуска. Это руководство поможет вам быстро начать работу с этими командами.

## Предварительные требования

- Права администратора в боте
- Базовое понимание регулярных выражений (для работы с правилами парсинга)

## Первые шаги

### 1. Проверка статуса

Начните с проверки текущего состояния системы:

```
/config_status
```

Вы увидите:
- Состояние системы
- Статус подключений
- Количество правил парсинга
- Параметры конфигурации

### 2. Просмотр правил парсинга

Посмотрите, какие правила парсинга уже настроены:

```
/list_parsing_rules
```

Вы увидите список всех правил с их параметрами.

### 3. Создание бэкапа

Перед любыми изменениями создайте бэкап:

```
/backup_config Мой первый бэкап
```

Сохраните ID бэкапа - он понадобится для восстановления.

## Основные операции

### Добавление нового правила парсинга

Допустим, вы хотите добавить парсинг для нового бота "TestGame":

```
/add_parsing_rule TestGame "Очки: +(\d+)" 1.0 points
```

Где:
- `TestGame` - название бота
- `"Очки: +(\d+)"` - паттерн для поиска
- `1.0` - множитель конвертации
- `points` - тип валюты

### Изменение множителя

Если нужно изменить множитель конвертации:

```
/update_parsing_rule 1 multiplier 2.0
```

Где:
- `1` - ID правила (из `/list_parsing_rules`)
- `multiplier` - поле для изменения
- `2.0` - новое значение

### Отключение правила

Чтобы временно отключить правило:

```
/update_parsing_rule 1 is_active false
```

### Включение обратно

```
/update_parsing_rule 1 is_active true
```

## Работа с конфигурацией

### Экспорт конфигурации

Сохраните текущую конфигурацию в файл:

```
/export_config
```

Бот отправит вам JSON файл. Сохраните его в безопасном месте.

### Импорт конфигурации

Чтобы загрузить конфигурацию из файла:

1. Отправьте JSON файл боту
2. В подписи к файлу напишите:
```
/import_config with-rules
```

### Валидация

После любых изменений проверьте корректность:

```
/validate_config
```

Если есть ошибки - исправьте их перед перезагрузкой.

### Перезагрузка

Примените изменения:

```
/reload_config
```

## Типичные сценарии

### Сценарий 1: Добавление нового бота

```bash
# 1. Создать бэкап
/backup_config Перед добавлением NewBot

# 2. Добавить правило
/add_parsing_rule NewBot "Баллы: (\d+)" 1.5 points

# 3. Проверить
/list_parsing_rules

# 4. Валидировать
/validate_config

# 5. Перезагрузить
/reload_config

# 6. Проверить статус
/config_status
```

### Сценарий 2: Изменение курса конвертации

```bash
# 1. Посмотреть текущие правила
/list_parsing_rules

# 2. Создать бэкап
/backup_config Перед изменением курса

# 3. Изменить множитель
/update_parsing_rule 2 multiplier 3.0

# 4. Перезагрузить
/reload_config

# 5. Проверить
/list_parsing_rules
```

### Сценарий 3: Восстановление после ошибки

```bash
# 1. Посмотреть бэкапы
/list_backups

# 2. Выбрать нужный и восстановить
/restore_config abc123def456

# 3. Проверить статус
/config_status

# 4. Валидировать
/validate_config
```

## Советы и лучшие практики

### 1. Всегда создавайте бэкапы

Перед любыми изменениями:
```
/backup_config Описание изменений
```

### 2. Валидируйте перед перезагрузкой

После изменений:
```
/validate_config
```

### 3. Проверяйте статус после изменений

```
/config_status
```

### 4. Используйте описательные имена для бэкапов

❌ Плохо:
```
/backup_config backup1
```

✅ Хорошо:
```
/backup_config Стабильная конфигурация перед добавлением 3 новых ботов
```

### 5. Тестируйте изменения

После добавления нового правила:
1. Проверьте, что оно активно
2. Отправьте тестовое сообщение
3. Проверьте, что парсинг работает

### 6. Храните экспорты конфигурации

Регулярно экспортируйте конфигурацию:
```
/export_config
```

Сохраняйте файлы с датой в имени.

## Частые ошибки

### Ошибка: "Неверный формат регулярного выражения"

**Проблема:** Паттерн содержит синтаксическую ошибку.

**Решение:**
1. Проверьте паттерн на сайте regex101.com
2. Убедитесь, что используете правильный синтаксис Python regex
3. Экранируйте специальные символы

**Пример:**
```
# Неправильно
/add_parsing_rule Bot "Очки: +(\d+" 1.0 points

# Правильно
/add_parsing_rule Bot "Очки: +(\d+)" 1.0 points
```

### Ошибка: "Правило с таким паттерном уже существует"

**Проблема:** Вы пытаетесь добавить дубликат.

**Решение:**
1. Проверьте существующие правила: `/list_parsing_rules`
2. Обновите существующее правило вместо создания нового
3. Или используйте другой паттерн

### Ошибка: "Не удалось перезагрузить конфигурацию"

**Проблема:** Конфигурация содержит ошибки.

**Решение:**
1. Проверьте валидацию: `/validate_config`
2. Исправьте ошибки
3. Попробуйте снова: `/reload_config`
4. Если не помогает - восстановите из бэкапа

## Дополнительные ресурсы

- [Полная документация конфигурационных команд](CONFIGURATION_COMMANDS.md)
- [Инструкция по парсингу](ИНСТРУКЦИЯ_ПАРСИНГ.md)
- [Основная документация](README.md)

## Получение помощи

Если у вас возникли проблемы:

1. Проверьте логи ошибок:
   ```
   /admin_errors
   ```

2. Проверьте здоровье системы:
   ```
   /admin_health
   ```

3. Обратитесь к администратору

4. Создайте issue в репозитории проекта

## Заключение

Конфигурационные команды - мощный инструмент для управления ботом. Следуйте этому руководству, и вы быстро освоите их использование.

**Помните:**
- Всегда создавайте бэкапы
- Валидируйте изменения
- Проверяйте статус после изменений
- Храните экспорты конфигурации

Удачи! 🚀
