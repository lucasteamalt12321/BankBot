# Временный парсер сообщений

Скрипт для парсинга сообщений из игровых ботов (рыбалка и карточные игры).

## Файлы

- `temp_message_parser.py` - основной парсер
- `demo_parser.py` - демонстрация возможностей
- `test_with_real_examples.py` - тестирование на реальных примерах
- `parsed_results.json` - результаты парсинга (создается автоматически)

## Поддерживаемые типы сообщений

### 🎣 Рыбалка
- Извлекает: пользователя, очки, опыт, улов, погоду, место, энергию
- Источник: `shmalala`
- Тип активности: `fishing`

### 🃏 Карточные игры  
- Извлекает: пользователя, очки, название карты, редкость, коллекцию
- Источник: `gdcards`
- Типы активности: `card_common`, `card_rare`, `card_epic`, `card_legendary`

## Использование

### Запуск основного скрипта
```bash
python temp_message_parser.py
```

### Демонстрация возможностей
```bash
python demo_parser.py
```

### Тестирование на примерах
```bash
python test_with_real_examples.py
```

## API

### Парсинг одного сообщения
```python
from temp_message_parser import parse_single_message

activities = parse_single_message(message_text)
```

### Парсинг из файла
```python
from temp_message_parser import parse_from_file

activities = parse_from_file('path/to/file.txt')
```

### Сохранение в JSON
```python
from temp_message_parser import save_results_to_json

save_results_to_json(activities, 'output.json')
```

### Статистика пользователей
```python
from temp_message_parser import get_user_statistics

stats = get_user_statistics(activities)
```

## Структура ParsedActivity

```python
@dataclass
class ParsedActivity:
    user_identifier: str    # Имя пользователя
    activity_type: str      # Тип активности
    points: int            # Количество очков
    game_source: str       # Источник игры
    metadata: Dict         # Дополнительные данные
```

## Результаты тестирования

✅ Все примеры из `parsing_examples.txt` успешно распознаны (100% успех)
✅ Поддержка различных форматов сообщений
✅ Извлечение всех ключевых данных
✅ Сохранение в JSON формате
✅ Статистика по пользователям

## Особенности

- Не использует файлы из папки `for_programmer`
- Автоматическое определение типа сообщения
- Обработка эмодзи и специальных символов
- Извлечение метаданных (улов, погода, редкость карт)
- Пакетная обработка сообщений
- Статистика активности пользователей