# Система проверки прав администратора

Эта документация описывает систему проверки прав администратора для Telegram бота, реализованную в рамках задачи 2 спецификации `telegram-bot-admin-system`.

## Обзор

Система предоставляет:
- Функцию `is_admin()` для проверки прав пользователя
- Декоратор `admin_required` для защиты административных команд
- Обработку ошибок доступа с понятными сообщениями
- Простую базу данных для хранения пользователей и транзакций

## Основные компоненты

### 1. AdminSystem

Основной класс для управления правами администратора.

```python
from utils.admin_system import AdminSystem

admin_system = AdminSystem("data/bot.db")
```

#### Основные методы:

- `is_admin(user_id: int) -> bool` - проверка прав администратора
- `register_user(user_id, username, first_name) -> bool` - регистрация пользователя
- `get_user_by_username(username) -> dict` - поиск пользователя по username
- `update_balance(user_id, amount) -> float` - обновление баланса
- `set_admin_status(user_id, is_admin) -> bool` - назначение/снятие прав администратора
- `get_users_count() -> int` - получение количества пользователей
- `add_transaction(user_id, amount, type, admin_id) -> int` - добавление транзакции

### 2. Декоратор admin_required

Защищает команды от доступа неавторизованных пользователей.

```python
from utils.admin_system import admin_required

@admin_required(admin_system)
async def admin_command(update: Update, context: ContextTypes.DEFAULT_TYPE):
    # Код команды, доступной только администраторам
    await update.message.reply_text("Вы администратор!")
```

### 3. Исключения

Система предоставляет специальные исключения для обработки ошибок:

- `PermissionError` - ошибки доступа
- `UserNotFoundError` - пользователь не найден
- `InsufficientBalanceError` - недостаточный баланс

## Структура базы данных

### Таблица users
```sql
CREATE TABLE users (
    id INTEGER PRIMARY KEY,              -- Telegram ID
    username TEXT,                       -- @username без @
    first_name TEXT,                     -- Имя пользователя
    balance REAL DEFAULT 0,              -- Баланс очков
    is_admin BOOLEAN DEFAULT FALSE       -- Флаг администратора
);
```

### Таблица transactions
```sql
CREATE TABLE transactions (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER,                     -- Ссылка на users.id
    amount REAL,                         -- Сумма транзакции
    type TEXT,                          -- 'add', 'remove', 'buy'
    admin_id INTEGER,                   -- ID администратора
    timestamp DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users (id),
    FOREIGN KEY (admin_id) REFERENCES users (id)
);
```

## Интеграция с существующим ботом

### Шаг 1: Импорт системы

```python
from utils.admin_system import AdminSystem, admin_required
from bot.admin_commands import AdminCommands
```

### Шаг 2: Инициализация в классе бота

```python
class TelegramBot:
    def __init__(self):
        self.admin_system = AdminSystem("data/bot.db")
        self.admin_commands = AdminCommands("data/bot.db")
```

### Шаг 3: Добавление обработчиков команд

```python
def setup_handlers(self):
    # Административные команды
    self.application.add_handler(CommandHandler("admin", self.admin_commands.admin_panel_command))
    self.application.add_handler(CommandHandler("add_points", self.admin_commands.add_points_command))
    self.application.add_handler(CommandHandler("add_admin", self.admin_commands.add_admin_command))
    
    # Обработчик для автоматической регистрации
    self.application.add_handler(MessageHandler(
        filters.TEXT & ~filters.COMMAND,
        self.handle_message
    ))
```

### Шаг 4: Автоматическая регистрация пользователей

```python
async def handle_message(self, update: Update, context: ContextTypes.DEFAULT_TYPE):
    user = update.effective_user
    
    # Автоматически регистрируем пользователя
    self.admin_system.register_user(
        user.id,
        user.username,
        user.first_name
    )
```

## Административные команды

### /admin
Показывает панель администратора с доступными командами и статистикой.

**Доступ:** Только администраторы

**Пример ответа:**
```
🔧 Панель администратора

👋 Добро пожаловать, Admin!

📊 Статистика:
   • Всего пользователей: 42

🛠️ Доступные команды:
   • /add_points @username [число] - начислить очки
   • /add_admin @username - добавить администратора
```

### /add_points @username [количество]
Начисляет очки указанному пользователю.

**Доступ:** Только администраторы

**Примеры:**
- `/add_points @john_doe 100`
- `/add_points user123 50`

**Пример ответа:**
```
✅ Очки успешно начислены!

👤 Пользователь: @john_doe
💰 Начислено: 100 очков
💳 Новый баланс: 150 очков
📝 ID транзакции: 42
```

### /add_admin @username
Назначает пользователя администратором.

**Доступ:** Только администраторы

**Примеры:**
- `/add_admin @new_admin`
- `/add_admin user456`

**Пример ответа:**
```
✅ Администратор назначен!

👤 Пользователь: @new_admin
🔧 Статус: Администратор
👑 Назначен: @main_admin
```

## Обработка ошибок

### Ошибки доступа
Когда обычный пользователь пытается использовать административную команду:

```
🔒 У вас нет прав администратора для выполнения этой команды.
Обратитесь к администратору бота для получения доступа.
```

### Пользователь не найден
Когда указанный пользователь не существует в системе:

```
❌ Пользователь @nonexistent не найден
```

### Неверный формат команды
Когда команда используется с неправильными параметрами:

```
❌ Неверный формат команды

Используйте: /add_points @username [количество]

Примеры:
• /add_points @john_doe 100
• /add_points user123 50
```

## Создание первого администратора

Для создания первого администратора используйте функцию `create_initial_admin`:

```python
from utils.admin_system import AdminSystem

admin_system = AdminSystem("data/bot.db")

# Создаем первого администратора
create_initial_admin(
    admin_system,
    YOUR_TELEGRAM_ID,  # Ваш Telegram ID
    "your_username",   # Ваш username
    "Your Name"        # Ваше имя
)
```

## Логирование

Система автоматически логирует все административные действия:

```
INFO - Admin 123456789 added 100.0 points to user 987654321
INFO - Admin 123456789 granted admin rights to user 555555555
WARNING - User 999999999 (@regular_user) attempted to use admin command without permissions
```

## Тестирование

Система включает полный набор тестов:

```bash
python tests/test_admin_system.py
```

Тесты покрывают:
- Проверку прав администратора
- Регистрацию пользователей
- Обновление балансов
- Работу декоратора admin_required
- Обработку ошибок

## Безопасность

### Рекомендации по безопасности:

1. **Ограничьте количество администраторов** - назначайте права только доверенным пользователям
2. **Регулярно проверяйте логи** - отслеживайте административные действия
3. **Используйте резервное копирование** - регулярно создавайте бэкапы базы данных
4. **Валидируйте входные данные** - система автоматически проверяет параметры команд

### Защита от атак:

- **SQL-инъекции**: Используются параметризованные запросы
- **Переполнение**: Ограничения на размер входных данных
- **Брутфорс**: Логирование всех попыток доступа

## Совместимость

Система совместима с:
- Существующей архитектурой бота
- SQLAlchemy ORM (работает параллельно)
- Любыми версиями python-telegram-bot >= 20.0

## Производительность

- **Быстрые запросы**: Простые SQL-запросы без сложных JOIN
- **Минимальная нагрузка**: Легковесная архитектура
- **Масштабируемость**: Готовность к переходу на PostgreSQL

## Поддержка

При возникновении проблем:

1. Проверьте логи бота
2. Убедитесь, что база данных доступна
3. Проверьте права доступа к файлу базы данных
4. Запустите тесты для диагностики

## Примеры использования

Полные примеры интеграции доступны в файле `examples/admin_integration_example.py`.