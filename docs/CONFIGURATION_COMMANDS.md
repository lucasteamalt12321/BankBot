# Конфигурационные команды бота

## Обзор

Конфигурационные команды позволяют администраторам управлять настройками бота без перезапуска. Все команды требуют прав администратора.

## Список команд

### /reload_config

Перезагружает конфигурацию бота без перезапуска.

**Использование:**
```
/reload_config
```

**Требования:** Права администратора

**Что перезагружается:**
- Правила парсинга
- Список администраторов
- Настройки системы
- Параметры фоновых задач

**Пример:**
```
/reload_config
```

**Ответ:**
```
✅ Конфигурация успешно перезагружена!

📊 Статистика:
• Правила парсинга: 4
• Администраторы: 2
• Время перезагрузки: 14:30:25
```

---

### /config_status

Показывает текущий статус конфигурации и здоровье системы.

**Использование:**
```
/config_status
```

**Требования:** Права администратора

**Отображаемая информация:**
- Состояние системы (здорово/проблемы)
- Статус подключения к БД
- Статус парсинга
- Статус фоновых задач
- Параметры конфигурации
- Время последней перезагрузки
- Ошибки валидации (если есть)

**Пример:**
```
/config_status
```

**Ответ:**
```
✅ Статус конфигурации

🏥 Состояние системы:
• Общее состояние: ✅ Здорово
• База данных: ✅ Подключена
• Парсинг: ✅ Активен
• Фоновые задачи: ✅ Работают

⚙️ Параметры конфигурации:
• Правила парсинга: 4
• Администраторы: 2
• Интервал очистки стикеров: 300с
• Задержка удаления стикеров: 86400с
• Размер пакета рассылки: 30
• Максимум попыток парсинга: 3

🕐 Последняя перезагрузка: 20.02.2026 14:30:25
```

---

### /list_parsing_rules

Показывает список всех правил парсинга.

**Использование:**
```
/list_parsing_rules
```

**Требования:** Права администратора

**Отображаемая информация:**
- ID правила
- Название бота
- Паттерн регулярного выражения
- Множитель конвертации
- Тип валюты
- Статус (активно/неактивно)

**Пример:**
```
/list_parsing_rules
```

**Ответ:**
```
📋 Правила парсинга (4):

✅ 1. Shmalala
   • ID: 1
   • Паттерн: `Очки: +(\d+)`
   • Множитель: 1.0
   • Тип валюты: points
   • Статус: Активно

✅ 2. GD Cards
   • ID: 2
   • Паттерн: `Монеты: +(\d+)`
   • Множитель: 2.0
   • Тип валюты: coins
   • Статус: Активно

...
```

---

### /add_parsing_rule

Добавляет новое правило парсинга.

**Использование:**
```
/add_parsing_rule <bot_name> <pattern> <multiplier> <currency_type>
```

**Параметры:**
- `bot_name` - название бота (без пробелов)
- `pattern` - регулярное выражение для парсинга (в кавычках)
- `multiplier` - множитель конвертации (число)
- `currency_type` - тип валюты (points, coins, etc.)

**Требования:** Права администратора

**Примеры:**
```
/add_parsing_rule TestBot "Очки: +(\d+)" 1.5 points
/add_parsing_rule NewGame "Монеты: (\d+)" 2.0 coins
```

**Ответ:**
```
✅ Правило парсинга успешно добавлено!

🤖 Бот: TestBot
🔍 Паттерн: `Очки: +(\d+)`
💰 Множитель: 1.5
💎 Тип валюты: points

Правило активно и готово к использованию.
```

---

### /update_parsing_rule

Обновляет существующее правило парсинга.

**Использование:**
```
/update_parsing_rule <rule_id> <field> <value>
```

**Параметры:**
- `rule_id` - ID правила (число)
- `field` - поле для обновления
- `value` - новое значение

**Доступные поля:**
- `bot_name` - название бота
- `pattern` - регулярное выражение
- `multiplier` - множитель конвертации
- `currency_type` - тип валюты
- `is_active` - статус (true/false)

**Требования:** Права администратора

**Примеры:**
```
/update_parsing_rule 1 multiplier 2.0
/update_parsing_rule 2 is_active false
/update_parsing_rule 3 pattern "Новый паттерн: (\d+)"
```

**Ответ:**
```
✅ Правило парсинга успешно обновлено!

🆔 ID правила: 1
📝 Поле: multiplier
🔄 Новое значение: 2.0

Изменения применены немедленно.
```

---

### /export_config

Экспортирует текущую конфигурацию в JSON файл.

**Использование:**
```
/export_config [no-rules]
```

**Параметры:**
- `no-rules` (опционально) - исключить правила парсинга из экспорта

**Требования:** Права администратора

**Примеры:**
```
/export_config
/export_config no-rules
```

**Ответ:**
Бот отправит JSON файл с конфигурацией:
```
📤 Экспорт конфигурации
🕒 Создан: 20.02.2026 14:30:25
📋 Правила парсинга: Включены
```

**Формат файла:**
```json
{
  "version": "1.0",
  "timestamp": "2026-02-20T14:30:25Z",
  "admin_user_ids": [123456789],
  "parsing_rules": [...],
  "system_settings": {...}
}
```

---

### /import_config

Импортирует конфигурацию из JSON файла.

**Использование:**
1. Отправьте JSON файл конфигурации
2. В подписи к файлу напишите `/import_config` или `/import_config with-rules`

**Параметры:**
- `with-rules` (опционально) - импортировать правила парсинга

**Требования:** Права администратора

**Примеры:**
```
/import_config
/import_config with-rules
```

**Ответ:**
```
✅ Конфигурация успешно импортирована!

📋 Правила парсинга: Импортированы
🔄 Конфигурация перезагружена.
🕒 Время: 20.02.2026 14:30:25
```

---

### /backup_config

Создает бэкап текущей конфигурации.

**Использование:**
```
/backup_config [description]
```

**Параметры:**
- `description` (опционально) - описание бэкапа

**Требования:** Права администратора

**Примеры:**
```
/backup_config
/backup_config Перед обновлением правил парсинга
/backup_config Стабильная конфигурация 20.02.2026
```

**Ответ:**
```
✅ Бэкап конфигурации создан успешно!

🆔 ID бэкапа: `abc123def456`
📝 Описание: Перед обновлением правил парсинга
🕒 Создан: 20.02.2026 14:30:25

Используйте `/restore_config abc123def456` для восстановления.
```

---

### /restore_config

Восстанавливает конфигурацию из бэкапа.

**Использование:**
```
/restore_config <backup_id>
```

**Параметры:**
- `backup_id` - ID бэкапа (получить через `/list_backups`)

**Требования:** Права администратора

**Примеры:**
```
/restore_config abc123def456
```

**Ответ:**
```
✅ Конфигурация восстановлена успешно!

🆔 ID бэкапа: `abc123def456`
🔄 Конфигурация перезагружена.
🕒 Восстановлено: 20.02.2026 14:35:00
```

---

### /list_backups

Показывает список доступных бэкапов конфигурации.

**Использование:**
```
/list_backups
```

**Требования:** Права администратора

**Пример:**
```
/list_backups
```

**Ответ:**
```
📂 Бэкапы конфигурации (5):

1. abc123de...
   📝 Перед обновлением правил парсинга
   🕒 20.02.2026 14:30:25
   📊 15.3 КБ

2. def456ab...
   📝 Стабильная конфигурация
   🕒 19.02.2026 10:15:00
   📊 14.8 КБ

3. ghi789cd...
   📝 Ручной бэкап от администратора 123456789
   🕒 18.02.2026 16:45:30
   📊 15.1 КБ

...

Используйте `/restore_config <backup_id>` для восстановления.
```

---

### /validate_config

Проверяет корректность текущей конфигурации.

**Использование:**
```
/validate_config
```

**Требования:** Права администратора

**Проверки:**
- Корректность правил парсинга
- Валидность регулярных выражений
- Корректность параметров системы
- Наличие обязательных полей
- Соответствие схеме конфигурации

**Пример:**
```
/validate_config
```

**Ответ (успех):**
```
✅ Валидация конфигурации пройдена

📋 Правила парсинга: 4
👥 Администраторы: 2
🧹 Интервал очистки: 300с
⏰ Задержка удаления: 86400с
📤 Размер пакета: 30
🔄 Макс. попыток: 3

Все настройки конфигурации корректны! ✨
```

**Ответ (ошибки):**
```
❌ Валидация конфигурации не пройдена

Найдено 3 ошибок валидации:

• Правило парсинга #2: некорректное регулярное выражение
• Параметр sticker_cleanup_interval: значение должно быть > 0
• Отсутствует обязательное поле: admin_user_ids

Пожалуйста, исправьте эти проблемы и перезагрузите конфигурацию.
```

---

## Рабочий процесс

### Типичный сценарий обновления конфигурации

1. **Создать бэкап:**
   ```
   /backup_config Перед изменениями
   ```

2. **Проверить текущий статус:**
   ```
   /config_status
   ```

3. **Внести изменения:**
   ```
   /add_parsing_rule NewBot "Pattern: (\d+)" 1.0 points
   ```

4. **Валидировать:**
   ```
   /validate_config
   ```

5. **Перезагрузить:**
   ```
   /reload_config
   ```

6. **Проверить результат:**
   ```
   /list_parsing_rules
   ```

### Восстановление после ошибки

1. **Посмотреть список бэкапов:**
   ```
   /list_backups
   ```

2. **Восстановить последний рабочий:**
   ```
   /restore_config abc123def456
   ```

3. **Проверить статус:**
   ```
   /config_status
   ```

---

## Безопасность

### Права доступа

Все конфигурационные команды требуют прав администратора. Попытка использования команды без прав приведет к сообщению:

```
❌ Доступ запрещен

Эта команда доступна только администраторам.
```

### Логирование

Все административные действия логируются:
- Кто выполнил команду
- Когда выполнена
- Какие параметры использованы
- Результат выполнения

### Бэкапы

- Бэкапы создаются автоматически перед критическими операциями
- Рекомендуется создавать ручные бэкапы перед изменениями
- Бэкапы хранятся в безопасном месте
- Можно восстановить любой предыдущий бэкап

---

## Troubleshooting

### Проблема: Конфигурация не перезагружается

**Решение:**
1. Проверьте логи: `/admin_errors`
2. Проверьте статус: `/config_status`
3. Валидируйте конфигурацию: `/validate_config`
4. Если есть ошибки - исправьте их
5. Попробуйте снова: `/reload_config`

### Проблема: Правило парсинга не работает

**Решение:**
1. Проверьте список правил: `/list_parsing_rules`
2. Убедитесь, что правило активно
3. Проверьте регулярное выражение
4. Проверьте множитель и тип валюты
5. Перезагрузите конфигурацию: `/reload_config`

### Проблема: Не могу импортировать конфигурацию

**Решение:**
1. Проверьте формат JSON файла
2. Убедитесь, что файл не поврежден
3. Проверьте, что все обязательные поля присутствуют
4. Валидируйте файл перед импортом
5. Попробуйте экспортировать текущую конфигурацию для примера

### Проблема: Бэкап не восстанавливается

**Решение:**
1. Проверьте список бэкапов: `/list_backups`
2. Убедитесь, что используете правильный ID
3. Проверьте, что бэкап не поврежден
4. Попробуйте другой бэкап
5. Обратитесь к разработчику, если проблема сохраняется

---

## Примеры использования

### Добавление нового бота для парсинга

```
# 1. Создать бэкап
/backup_config Перед добавлением нового бота

# 2. Добавить правило
/add_parsing_rule NewGameBot "Баллы: +(\d+)" 1.5 points

# 3. Проверить
/list_parsing_rules

# 4. Валидировать
/validate_config

# 5. Перезагрузить
/reload_config
```

### Изменение множителя конвертации

```
# 1. Посмотреть текущие правила
/list_parsing_rules

# 2. Обновить множитель
/update_parsing_rule 2 multiplier 3.0

# 3. Перезагрузить
/reload_config

# 4. Проверить
/list_parsing_rules
```

### Экспорт и импорт конфигурации

```
# Экспорт
/export_config

# Сохраните полученный файл

# Импорт (отправьте файл с подписью)
/import_config with-rules
```

---

## Дополнительная информация

### Связанные команды

- `/admin_parsing_reload` - альтернативная команда перезагрузки
- `/admin_parsing_config` - альтернативная команда просмотра конфигурации
- `/admin_health` - общее здоровье системы
- `/admin_backup` - бэкап базы данных

### Документация

- [Основная документация](README.md)
- [Инструкция по парсингу](ИНСТРУКЦИЯ_ПАРСИНГ.md)
- [Миграция конфигурации](CONFIG_MIGRATION.md)

### Поддержка

При возникновении проблем:
1. Проверьте документацию
2. Проверьте логи: `/admin_errors`
3. Обратитесь к администратору
4. Создайте issue в репозитории
