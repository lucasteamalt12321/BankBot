# Функция ручного парсинга сообщений

## Описание изменений

Добавлена новая функция для ручной обработки сообщений от игровых ботов по команде "парсинг".

## Что было добавлено

### 1. Обработчик команды "парсинг" (bot/bot.py)

**Изменения в методе `parse_all_messages`:**
```python
# Проверяем, является ли это командой "парсинг" в ответ на сообщение
if message_text and message_text.strip().lower() == "парсинг" and update.message.reply_to_message:
    await self.handle_manual_parsing(update, context)
    return
```

**Новый метод `handle_manual_parsing`:**
- Проверяет, что команда используется в ответ на сообщение от бота
- Парсит содержимое сообщения с помощью существующих парсеров
- Начисляет монеты/очки пользователю
- Отправляет подробное подтверждение

### 2. Документация

Созданы следующие файлы документации:

- **MANUAL_PARSING_GUIDE.md** - полное руководство пользователя
  - Описание функции
  - Пошаговые инструкции
  - Примеры использования
  - Обработка ошибок
  - FAQ

- **MANUAL_PARSING_VISUAL_EXAMPLE.md** - визуальные примеры
  - Диаграммы процесса
  - Примеры сообщений
  - Схема взаимодействия

- **QUICK_PARSING_CHEATSHEET.md** - краткая шпаргалка
  - Быстрый старт
  - Таблица поддерживаемых ботов
  - Частые ошибки

### 3. Примеры кода

**examples/manual_parsing_example.py:**
- Тестовые сообщения для парсинга
- Функция для проверки работы парсера
- Примеры использования API

## Как использовать

### Для пользователей

1. Найдите сообщение от игрового бота (Shmalala, GD Cards и т.д.)
2. Нажмите "Ответить" на это сообщение
3. Напишите: `парсинг`
4. Получите подтверждение о начислении монет

### Для разработчиков

```python
# Метод вызывается автоматически при получении команды "парсинг"
async def handle_manual_parsing(self, update: Update, context: ContextTypes.DEFAULT_TYPE):
    # 1. Проверка, что это ответ на сообщение от бота
    # 2. Парсинг содержимого
    # 3. Начисление монет
    # 4. Отправка подтверждения
```

## Технические детали

### Безопасность

- ✅ Проверка, что команда используется в ответ на сообщение
- ✅ Проверка, что исходное сообщение от бота
- ✅ Валидация формата сообщения
- ✅ Логирование всех операций
- ✅ Обработка ошибок с информативными сообщениями

### Интеграция

Функция использует существующую инфраструктуру:
- `core.parsers.simple_parser.parse_game_message()` - парсинг сообщений
- `database.database.User` - работа с пользователями
- `structlog` - логирование
- Telegram Bot API - получение reply_to_message

### Логирование

Все операции логируются с подробной информацией:
```python
logger.info(
    "Manual parsing requested",
    user_id=user.id,
    bot_name=bot_user.first_name,
    message_preview=message_text[:100]
)
```

## Преимущества

1. **Гибкость**: Можно обработать любое сообщение в любое время
2. **Надежность**: Не зависит от автоматических триггеров
3. **Прозрачность**: Подробная информация о результате
4. **Простота**: Одна команда для всех типов сообщений
5. **Восстановление**: Возможность обработать пропущенные сообщения

## Поддерживаемые форматы

### Shmalala - Рыбалка
```
🎣 [Рыбалка] 🎣
Рыбак: <username>
Монеты: +<amount> (<total>)💰
```

### GD Cards - Новая карта
```
🃏 НОВАЯ КАРТА 🃏
Игрок: <username>
Очки: +<amount>
```

## Будущие улучшения

Возможные расширения функционала:
- Поддержка дополнительных игровых ботов
- Пакетная обработка нескольких сообщений
- Статистика ручного парсинга
- Настройка коэффициентов конвертации
- История обработанных сообщений

## Связанные файлы

### Основной код
- `bot/bot.py` - основная логика бота
- `core/parsers/simple_parser.py` - парсеры сообщений

### Документация
- `docs/MANUAL_PARSING_GUIDE.md` - полное руководство
- `docs/MANUAL_PARSING_VISUAL_EXAMPLE.md` - визуальные примеры
- `docs/QUICK_PARSING_CHEATSHEET.md` - шпаргалка
- `README.md` - обновлен с информацией о функции

### Примеры
- `examples/manual_parsing_example.py` - тестовый пример

## Тестирование

Функция протестирована на:
- ✅ Сообщениях от Shmalala (рыбалка)
- ✅ Сообщениях от GD Cards (новые карты)
- ✅ Обработке ошибок
- ✅ Валидации входных данных
- ✅ Логировании операций

## Заключение

Функция ручного парсинга успешно интегрирована в бота и готова к использованию. Она предоставляет пользователям дополнительный контроль над процессом начисления монет и позволяет обрабатывать сообщения, которые могли быть пропущены автоматическим парсером.
