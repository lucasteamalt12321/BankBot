# Test Environment Configuration
# Copy this file to .env.test and update with your values

# Environment
ENV=test

# Telegram Bot Configuration (use test bot token)
BOT_TOKEN=your_test_bot_token_here
ADMIN_TELEGRAM_ID=123456789

# Database (in-memory SQLite for tests)
DATABASE_URL=sqlite:///:memory:

# Parsing (disabled in tests)
PARSING_ENABLED=false

# Logging (minimal in tests)
LOG_LEVEL=WARNING
LOG_FILE=

# Background Tasks (faster intervals for testing)
TASK_CHECK_INTERVAL=60
