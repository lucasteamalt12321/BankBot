# Руководство по тестированию бота

## Быстрый старт

### 1. Запуск бота

```bash
# Убедитесь, что все зависимости установлены
pip install -r requirements.txt

# Запустите бота
python run_bot.py
```

### 2. Первоначальная настройка

1. Отправьте `/start` боту в Telegram
2. Убедитесь, что вы зарегистрированы как администратор
3. Проверьте статус системы: `/config_status`

## Тестирование конфигурационных команд

### Базовые проверки

#### 1. Проверка прав доступа

**От имени обычного пользователя:**
```
/reload_config
```
**Ожидается:** Сообщение об отказе в доступе

**От имени администратора:**
```
/reload_config
```
**Ожидается:** Успешная перезагрузка конфигурации

#### 2. Просмотр статуса

```
/config_status
```
**Ожидается:** Отображение:
- Состояние системы
- Статус подключений
- Параметры конфигурации

#### 3. Список правил парсинга

```
/list_parsing_rules
```
**Ожидается:** Список всех настроенных правил

### Работа с правилами парсинга

#### 4. Добавление правила

```
/add_parsing_rule TestBot "Очки: +(\d+)" 1.5 points
```
**Ожидается:** Сообщение об успешном добавлении

#### 5. Проверка добавленного правила

```
/list_parsing_rules
```
**Ожидается:** TestBot в списке правил

#### 6. Обновление правила

Сначала найдите ID правила из `/list_parsing_rules`, затем:
```
/update_parsing_rule <ID> multiplier 2.0
```
**Ожидается:** Сообщение об успешном обновлении

#### 7. Отключение правила

```
/update_parsing_rule <ID> is_active false
```
**Ожидается:** Правило отключено

#### 8. Включение обратно

```
/update_parsing_rule <ID> is_active true
```
**Ожидается:** Правило включено

### Работа с бэкапами

#### 9. Создание бэкапа

```
/backup_config Тестовый бэкап
```
**Ожидается:** 
- Сообщение с ID бэкапа
- Сохраните ID для следующих шагов

#### 10. Список бэкапов

```
/list_backups
```
**Ожидается:** Список всех бэкапов с вашим тестовым

#### 11. Восстановление из бэкапа

```
/restore_config <backup_id>
```
**Ожидается:** Сообщение об успешном восстановлении

### Экспорт и импорт

#### 12. Экспорт конфигурации

```
/export_config
```
**Ожидается:** Файл JSON с конфигурацией

#### 13. Экспорт без правил

```
/export_config no-rules
```
**Ожидается:** Файл JSON без правил парсинга

#### 14. Импорт конфигурации

1. Отправьте ранее экспортированный файл
2. В подписи напишите:
```
/import_config with-rules
```
**Ожидается:** Сообщение об успешном импорте

### Валидация

#### 15. Валидация конфигурации

```
/validate_config
```
**Ожидается:** 
- Если нет ошибок: сообщение об успешной валидации
- Если есть ошибки: список ошибок

## Полный цикл тестирования

### Сценарий 1: Добавление и откат

```bash
# 1. Создать бэкап
/backup_config Перед тестом

# 2. Добавить тестовое правило
/add_parsing_rule TestBot "Test: (\d+)" 1.0 points

# 3. Проверить
/list_parsing_rules

# 4. Восстановить бэкап
/restore_config <backup_id>

# 5. Проверить, что правило удалено
/list_parsing_rules
```

### Сценарий 2: Изменение и проверка

```bash
# 1. Посмотреть текущие правила
/list_parsing_rules

# 2. Создать бэкап
/backup_config Перед изменением множителя

# 3. Изменить множитель
/update_parsing_rule 1 multiplier 3.0

# 4. Проверить изменение
/list_parsing_rules

# 5. Валидировать
/validate_config

# 6. Перезагрузить
/reload_config

# 7. Проверить статус
/config_status
```

### Сценарий 3: Экспорт и импорт

```bash
# 1. Экспортировать текущую конфигурацию
/export_config

# 2. Сохранить файл

# 3. Изменить что-то
/add_parsing_rule TempBot "Temp: (\d+)" 1.0 points

# 4. Импортировать сохраненную конфигурацию
# (отправить файл с подписью)
/import_config with-rules

# 5. Проверить, что изменения откатились
/list_parsing_rules
```

## Автоматические тесты

### Запуск всех тестов

```bash
pytest tests/test_config_commands.py -v
```

### Запуск конкретного теста

```bash
pytest tests/test_config_commands.py::TestReloadConfig::test_reload_config_success -v
```

### Запуск с покрытием

```bash
pytest tests/test_config_commands.py --cov=bot.commands.config_commands --cov-report=html
```

## Проверка логов

### Просмотр логов в реальном времени

```bash
tail -f logs/bot.log
```

### Поиск ошибок

```bash
grep ERROR logs/bot.log
```

### Поиск действий администратора

```bash
grep "Admin" logs/bot.log
```

## Проверка базы данных

### SQLite

```bash
sqlite3 data/bot.db

# Проверить правила парсинга
SELECT * FROM parsing_rules;

# Проверить бэкапы
SELECT * FROM config_backups;

# Выход
.quit
```

## Troubleshooting

### Проблема: Команды не отвечают

**Решение:**
1. Проверьте, что бот запущен
2. Проверьте логи на ошибки
3. Проверьте права администратора

### Проблема: "Нет прав администратора"

**Решение:**
1. Проверьте ADMIN_TELEGRAM_ID в config/.env
2. Используйте `/add_admin @username` от имени существующего админа
3. Проверьте базу данных

### Проблема: Ошибка при импорте конфигурации

**Решение:**
1. Проверьте формат JSON файла
2. Убедитесь, что файл не поврежден
3. Попробуйте экспортировать текущую конфигурацию для примера

### Проблема: Бэкап не восстанавливается

**Решение:**
1. Проверьте список бэкапов: `/list_backups`
2. Убедитесь, что используете правильный ID
3. Проверьте логи на ошибки

## Чек-лист перед продакшном

- [ ] Все команды отвечают
- [ ] Проверка прав работает
- [ ] Бэкапы создаются и восстанавливаются
- [ ] Экспорт/импорт работает
- [ ] Валидация работает
- [ ] Логирование работает
- [ ] Автоматические тесты проходят
- [ ] База данных настроена
- [ ] Права на файловую систему есть
- [ ] Документация актуальна

## Дополнительные ресурсы

- [Полная документация команд](docs/CONFIGURATION_COMMANDS.md)
- [Быстрый старт](docs/QUICK_START_CONFIG.md)
- [Основная документация](README.md)
- [Отчет о реализации](.kiro/specs/implement-all-bot-commands/IMPLEMENTATION_COMPLETE.md)

## Получение помощи

При возникновении проблем:

1. Проверьте документацию
2. Проверьте логи: `/admin_errors`
3. Проверьте здоровье системы: `/admin_health`
4. Обратитесь к администратору
5. Создайте issue в репозитории

---

**Удачного тестирования! 🚀**
