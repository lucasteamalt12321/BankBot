> LucasTeam:
# AGENTS.md

## Общие принципы

- Отвечать на русском языке и следовать инструкциям пользователя.
- Использовать пакетный менеджер `pip` (или `poetry` при наличии) для управления зависимостями.
- Всегда проверять код после внесения изменений с помощью линтера `ruff` и автоматически исправлять ошибки, если это возможно.
- Не проверять Markdown-файлы (`*.md`) с помощью линтеров.
- Не спрашивать разрешения перед просмотром файлов проекта, а сразу использовать инструменты для просмотра.
- Не предлагать код для нескольких вариантов решения. Автоматически выбирать лучшее на основе мировых практик и применять его.
- Никогда самостоятельно не изменять что-либо, кроме того что запланировано или попросил пользователь. Предлагать такие действия в конце отчета о выполненной задаче.
- Не коммитить изменения на GitHub самостоятельно. Только по команде пользователя.

---

## Система Memory Bank

Ты действуешь как инженер, который не обладает долгосрочной памятью. Твой единственный устойчивый контекст — директория `/memory_bank`. Поддерживай документацию в актуальном состоянии, синхронизируя её с кодом.

### 1. Фундаментальные правила (Policy)

- `docs/README.md` — единственный источник информации о высокоуровневой архитектуре.
- Канонический источник процента выполнения: `memory_bank/projectbrief.md`.
- В `memory_bank/projectbrief.md` обязателен раздел `## Project Deliverables`.
- Создать раздел автоматически, если он отсутствует, при любой задаче, связанной с прогрессом, планированием или синхронизацией memory_bank.
- `## Project Deliverables` — канонический список результатов, по которому считается процент выполнения. Не журнал сессии.
- Каждый deliverable обязан иметь: стабильный ID, краткое название, статус (`pending` / `in_progress` / `completed` / `blocked`), вес в процентах.
- Сумма весов всех deliverables = `100`.
- Процент выполнения считается только по `## Project Deliverables`.
- `activeContext.md`, `progress.md`, `Changelog` — операционный контекст и история, не источник процента.
- Каждый новый пункт ТЗ или scope change добавлять в `## Project Deliverables` до или одновременно с реализацией.
- После каждой задачи, влияющей на готовность — обновить статусы deliverables.
- Если deliverables не синхронизированы с кодом — сначала восстановить, потом считать процент. Иначе процент = `нет достоверных данных`.

### 2. Память проекта

- Перед началом любой работы перечитай `/memory_bank`, особенно `activeContext.md` и `progress.md`.
- `/memory_bank` — твоя операционная система. Без актуализации теряешь контекст.
- Любые изменения в архитектуре, маршрутах или модулях отражать в `docs/README.md`.

### 3. Контроль изменений

- `progress.md` всегда содержит `last_checked_commit` (хеш или дата).
- При каждом сеансе сравнивай: `git log <last_checked_commit>...`
- При изменении документации — обновить хеш.

### 4. Структура Memory Bank

Обязательные файлы в `/memory_bank`:

| Файл | Назначение |
|------|-----------|
| `projectbrief.md` | Цели, рамки проекта, Project Deliverables |
| `productContext.md` | Продуктовый контекст, проблемы пользователей |
| `activeContext.md` | Текущий фокус, активные решения и приоритеты |
| `systemPatterns.md` | Архитектура, паттерны, связи подсистем |
| `techContext.md` | Стек, окружение, библиотеки, CI/CD |
| `progress.md` | Статус, Known Issues, Changelog, last_checked_commit |

Дополнительно: `modules/*`, `ui_extension/*`, `other/*`

### 5. Рабочие режимы

**Режим А — Планирование** (крупные/архитектурные/рискованные задачи):
1. Прочитай `productContext.md` и `activeContext.md`.
2. Составь план: шаги, файлы, риски.
3. Не приступай без одобрения пользователя.

Для повседневных правок и точечных фиксов — выполнять сразу.

**Режим Б — Выполнение:**
1. Обнови `activeContext.md` — задача взята в работу.
2. Выполняй последовательно.
3. Обновляй память: CI/CD → `techContext.md`; новый функционал → `docs/README.md`; legacy → `other/` + `progress.md`.
4. Обнови `progress.md` (Changelog) и `last_checked_commit`.

**Режим В — Инициализация** (Memory Bank пуст):
1. Создай файлы-заглушки.
2. Составь карту директорий и точек входа.
3. Запиши в `techContext.md` и `systemPatterns.md`.

### 6. Чек-лист перед завершением сессии

1. Проверил `git status` на неожиданные изменения?
2. Обновил `progress.md` и `activeContext.md`?
3. Синхронизировал `docs/README.md` при изменении архитектуры?
4. Указал новый `last_checked_commit`?

---

## План разработки проекта: BankBot с Bridge-модулем

### Обзор проекта

**BankBot** — единый Telegram-бот на Python (aiogram), объединяющий два функциональных блока в одном процессе:

- **Bridge-модуль** — встроенный компонент двусторонней пересылки сообщений и медиаконтента между одним Telegram-чатом и одним VK-чатом в реальном времени. VK Long Poll работает в отдельном потоке (threading) внутри того же процесса. Цель: объединить участников двух платформ без необходимости переключаться между ними. **Приоритет: КРИТИЧЕСКИЙ — реализуется первым.**

- **Банковская система** — функциональность управления виртуальными балансами, начислением очков и покупками внутри чата. Требует комплексного рефакторинга: устранения уязвимостей безопасности, улучшения архитектуры, повышения надёжности и покрытия тестами. **Приоритет: ВЫСОКИЙ — реализуется вторым.**

Оба блока работают в рамках одного процесса, используют единую базу данных и единую конфигурацию.

### Архитектура проекта

BankBot — единый процесс на базе **aiogram** (asyncio event loop), в который встроен Bridge-модуль. VK Long Poll работает в отдельном потоке (`threading.Thread`), взаимодействуя с aiogram через `asyncio.run_coroutine_threadsafe`. Единая база данных (SQLite / PostgreSQL) хранит как банковские данные, так и состояние Bridge. Единый модуль конфигурации на базе Pydantic Settings управляет всеми параметрами.

```text
┌─────────────────────────────────────────────────────────┐
│                    Процесс BankBot                       │
│                                                          │
│  ┌──────────────────────┐   ┌────────────────────────┐  │
│  │   aiogram event loop  │   │  VK Long Poll Thread   │  │
│  │                       │   │  (threading.Thread)    │  │
│  │  Handlers / Routers   │◄──┤  vk_listener.py        │  │
│  │  Bridge Handlers      │   │  → run_coroutine_       │  │
│  │  Middleware           │   │    threadsafe()         │  │
│  └──────────┬────────────┘   └────────────────────────┘  │
│             │                                             │
│  ┌──────────▼────────────────────────────────────────┐   │
│  │              Единая БД (SQLite / PostgreSQL)       │   │
│  │   users | balances | transactions | bridge_state  │   │
│  └───────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘

Структура модулей (основные директории):

BankBot/
├── bot/
│   ├── main.py                    # точка входа: запуск aiogram + VK thread
│   ├── bot.py                     # инициализация Bot и Dispatcher
│   ├── router.py                  # регистрация всех роутеров
│   ├── commands/                  # команды банковской системы
│   └── bridge/                    # Bridge-модуль
│       ├── telegram_forwarder.py  # aiogram handler: TG → VK
│       ├── vk_listener.py         # Long Poll VK → TG (отдельный поток)
│       ├── vk_sender.py           # отправка сообщений в VK через vk_api
│       ├── media_handler.py       # скачивание и загрузка медиафайлов
│       ├── loop_guard.py          # фильтрация циклов пересылки
│       └── message_queue.py       # очередь исходящих сообщений + воркер
├── core/
│   ├── services/                  # Service Layer (бизнес-логика)
│   ├── repositories/              # Repository Layer (доступ к БД)
│   ├── middleware.py              # централизованная обработка ошибок
│   └── di.py                      # DI-контейнер
├── database/
│   ├── models.py                  # ORM-модели (SQLAlchemy)
│   ├── migrations/                # Alembic миграции
│   └── connection.py              # Connection Pool
├── config/
│   └── settings.py                # Pydantic Settings (единая конфигурация)
├── tests/
│   ├── unit/
│   ├── integration/
│   └── e2e/
├── Dockerfile
├── docker-compose.yml
├── requirements.txt
└── requirements-dev.txt

### Требования (кратко)

Полные требования описаны в requirements.md. Ниже приведены ключевые группы с указанием приоритетов:

Общие требования (О-1 – О-5)  
- Управление конфиденциальными данными (переменные окружения)  
- Управление зависимостями (фиксированные версии, разделение prod/dev)  
- Корректность импортов  
- Документация проекта (README, DEPLOYMENT, диаграммы)  
- Docstrings в публичных методах (Google style)

Bridge-модуль (Б-1 – Б-13)  
- Конфигурация Bridge (Б-1, критический)  
- Telegram → VK (Б-2, критический)  
- VK → Telegram (Б-3, критический)  
- Фильтрация циклов пересылки (Б-4, критический)  
- Пересылка медиа (Б-5, Б-6, высокий)  
- Очередь сообщений и управление лимитами (Б-7, высокий)  
- Обработка ошибок 429 (Б-8, высокий)  
- Сохранение состояния (Б-9, высокий)  
- Тестирование Bridge (Б-10, средний)  
- Деплой и контейнеризация (Б-11, средний)  
- Вебхуки (Б-12, низкий)  
- Логирование и мониторинг (Б-13, высокий)

Банковская система (BB-1 – BB-18)  
- Централизованная конфигурация (BB-1, высокий)  
- Слой репозиториев (BB-2, высокий)  
- Service Layer (BB-3, высокий)  
- Модульная структура (BB-4, высокий)  
- Централизованная обработка ошибок (BB-5, высокий)  
- Graceful Shutdown (BB-6, высокий)  
- ParserRegistry в БД (BB-7, средний)  
- Атомарность операций с балансом (BB-8, критический)  
- Connection Pooling (BB-9, высокий)  
- Защита от SQL-инъекций (BB-10, критический)  
- Система алиасов (BB-11, средний)  
- DI-контейнер (BB-12, высокий)  
- Аудит неиспользуемого кода (BB-13, средний)  
- Управление deprecated-кодом (BB-14, средний)  
- Объединение дублирующихся парсеров (BB-15, средний)  
- E2E тесты (BB-16, высокий)  
- Тесты безопасности и надёжности (BB-17, высокий)  
- Покрытие тестами 80%+ (BB-18, средний)

### Задачи реализации

Реализация ведётся в двух блоках: сначала Bridge-модуль (критический приоритет), затем рефакторинг банковской системы. Каждая фаза завершается чекпоинтом. Задачи, помеченные *, опциональны и могут быть пропущены для ускорения MVP.

#### 1. Инфраструктура и конфигурация
- [ ] 1.1 Создать/обновить config/settings.py с Pydantic Settings  
  (поля: BRIDGE_ENABLED, BRIDGE_TG_CHAT_ID, VK_TOKEN, VK_PEER_ID, BRIDGE_ADMIN_CHAT_ID; валидация; поддержка окружений)  
  *Требования: О-1, Б-1, BB-1*
- [ ] 1.2 Обновить requirements.txt и requirements-dev.txt  
  (добавить vk_api~=11.x, hypothesis, pytest-asyncio, pytest-cov, ruff)  
  *Требования: О-2*
- [ ] 1.3 Создать миграцию Alembic для таблицы bridge_state  
  *Требования: Б-9*

> LucasTeam:
#### 2. Bridge-модуль: ядро
- [ ] 2.1 Создать структуру bot/bridge/ и loop_guard.py  
  *Требования: Б-4*
- [ ]* 2.2 Написать property-тест для loop_guard (Свойство 2)  
- [ ] 2.3 Реализовать message_queue.py (класс MessageQueue с очередью и воркером)  
  *Требования: Б-7, Б-8*
- [ ]* 2.4 Написать unit-тесты для message_queue  
- [ ] 2.5 Реализовать vk_sender.py (отправка текста в VK с префиксом и меткой)  
  *Требования: Б-2, Б-13*
- [ ] 2.6 Реализовать telegram_forwarder.py (aiogram handler для TG → VK)  
  *Требования: Б-2, Б-4*
- [ ] 2.7 Реализовать vk_listener.py (класс VKListenerThread с Long Poll)  
  *Требования: Б-3, Б-4, Б-6*
- [ ] 2.8 Интегрировать Bridge в bot/main.py (запуск потока, graceful shutdown)  
  *Требования: Б-1, BB-6*

Чекпоинт — Bridge ядро  
Убедиться, что все тесты проходят. Проверить запуск бота с BRIDGE_ENABLED=false и BRIDGE_ENABLED=true.

#### 3. Bridge-модуль: медиа и надёжность
- [ ] 4.1 Реализовать media_handler.py — пересылка медиа TG → VK  
  *Требования: Б-5*
- [ ] 4.2 Реализовать пересылку медиа VK → TG в vk_listener.py  
  *Требования: Б-6*
- [ ] 4.3 Реализовать сохранение/восстановление состояния через bridge_state  
  *Требования: Б-9*
- [ ]* 2.2b Написать property-тест для loop_guard (Свойство 1)

#### 4. Тесты Bridge-модуля
- [ ]* 5.1 Написать unit-тесты для loop_guard  
- [ ]* 5.2 Написать интеграционные тесты пересылки текста (mock API)  
- [ ]* 5.3 Написать тесты пересылки медиафайлов (mock API)

Чекпоинт — Bridge полный  
Убедиться, что все тесты Bridge-модуля проходят менее чем за 30 секунд.

#### 5. Рефакторинг банковской системы: основа
- [ ] 7.1 Создать core/repositories/ — слой репозиториев  
  *Требования: BB-2, BB-10*
- [ ] 7.2 Создать core/services/ — Service Layer  
  *Требования: BB-3*
- [ ] 7.3 Создать core/di.py — DI-контейнер  
  *Требования: BB-12*
- [ ] 7.4 Разбить монолитный bot.py на модули по группам команд  
  *Требования: BB-4*

#### 6. Рефакторинг банковской системы: безопасность и надёжность
- [ ] 8.1 Реализовать Unit of Work для атомарных транзакций с балансом  
  *Требования: BB-8*
- [ ] 8.2 Настроить Connection Pooling в database/connection.py  
  *Требования: BB-9*
- [ ] 8.3 Реализовать core/middleware.py — централизованная обработка ошибок  
  *Требования: BB-5*
- [ ] 8.4 Провести аудит SQL-запросов и добавить параметризацию  
  *Требования: BB-10*

Чекпоинт — Банковская система: основа  
Убедиться, что все тесты проходят. Проверить запуск бота с банковскими командами.

#### 7. Рефакторинг банковской системы: качество кода
- [ ] 10.1 Перенести конфигурацию парсеров в БД (ParserRegistry)  
  *Требования: BB-7*
- [ ] 10.2 Объединить дублирующиеся парсеры  
  *Требования: BB-15*
- [ ] 10.3 Реализовать систему алиасов пользователей  
  *Требования: BB-11*
- [ ] 10.4 Провести аудит неиспользуемого кода (D&D, кланы)  
  *Требования: BB-13, BB-14*

#### 8. Тесты банковской системы
- [ ]* 11.1 Написать E2E тесты ключевых сценариев  
- [ ]* 11.2 Написать тесты SQL-инъекций  
- [ ]* 11.3 Написать тесты race conditions для баланса  
- [ ]* 11.4 Написать property-тест атомарности баланса (Свойство 3)  
- [ ]* 11.5 Написать property-тест round-trip парсера (Свойство 4)  
- [ ] 11.6 Настроить pytest-cov и довести coverage до 80%+  

Чекпоинт — Тесты  
Убедиться, что полный набор E2E тестов выполняется менее чем за 60 секунд.

#### 9. Деплой и документация
- [ ] 13.1 Написать Dockerfile и docker-compose.yml  
  *Требования: Б-11*
- [ ] 13.2 Добавить docstrings (Google style) во все публичные методы  
  *Требования: О-5*
- [ ] 13.3 Обновить README.md и создать DEPLOYMENT.md  
  *Требования: О-4*
- [ ]* 13.4 Настроить вебхуки Flask + Nginx + SSL (опционально)  
  *Требования: Б-12*

Финальный чекпоинт  
Убедиться, что все обязательные тесты проходят, линтер не выдаёт ошибок, бот запускается в Docker.

### Ключевые риски и минимизация

#### Bridge-модуль
- Блокировка API: строго соблюдать лимиты, обрабатывать 429 с экспоненциальной задержкой.  
- Потеря сообщений при перезапуске: сохранять last_tg_msg_id и last_vk_msg_id в bridge_state.  
- Циклы пересылки: добавлять метку [BOT] ко всем пересылаемым сообщениям, фильтровать их.  
- Утечка секретов: хранить токены только в переменных окружения, исключить .env из репозитория.

#### Банковская система
- Потеря данных при конкурентных операциях: Unit of Work с транзакциями и блокировками строк.  
- SQL-инъекции: параметризованные запросы, статический анализ.  
- Race conditions: тесты конкурентного изменения баланса, блокировки на уровне строк.  
- Непреднамеренное взаимодействие устаревшего кода с Bridge-модулем: аудит устаревших модулей до интеграции Bridge, интеграционные тесты.

---

Примечание: Полные версии требований, технического дизайна и детальных задач находятся в файлах requirements.md, design.md и tasks.md соответственно. При работе над проектом обязательно обращайся к этим документам для уточнения деталей.
```
