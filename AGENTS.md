
# AGENTS.md

## Общие принципы

- Отвечать на русском языке и следовать инструкциям пользователя.
- Использовать пакетный менеджер `bun` для управления зависимостями.
- Всегда проверять код после внесения изменений с помощью линтера `ruff` и автоматически исправлять ошибки, если это возможно.
- Не проверять Markdown-файлы (`*.md`) с помощью линтеров.
- Не спрашивать разрешения перед просмотром файлов проекта, а сразу использовать инструменты для просмотра.
- Не предлагать код для нескольких вариантов решения. Автоматически выбирать лучшее на основе мировых практик и применять его.
- Никогда самостоятельно не изменять что-либо, кроме того что запланировано или попросил пользователь. Предлагать такие действия в конце отчета о выполненной задаче.
- Коммитить изменения на GitHub только в следующих случаях:
  - Ты закончил большой блок кода, запустил тесты и убедился, что все корректно работает
  - Пользователь попросил тебя закоммитить
- Не создавать много файлов в корне! Должны быть только: .gitignore, AGENTS.md, bot.db, README.md, requirements.txt, run_bot.py. Также должны быть вложенные папки

---

## Memory Bank — Обязательные правила

### Project Deliverables
- В `memory_bank/projectbrief.md` обязан быть раздел `## Project Deliverables`
- Таблица с колонками: `ID | Deliverable | Status | Weight`
- Status: только `pending`, `in_progress`, `completed`, `blocked`
- **Сумма Weight должна быть ровно 100** (пересчитывать после любого изменения)

### Процент выполнения
- Считается только по `## Project Deliverables`
- Если сумма ≠ 100 — процент невалиден


---

## Система Memory Bank

Ты действуешь как инженер, который не обладает долгосрочной памятью. Твой единственный устойчивый контекст — директория `/memory_bank`. Поддерживай документацию в актуальном состоянии, синхронизируя её с кодом.

### 1. Фундаментальные правила (Policy)

- `docs/README.md` — единственный источник информации о высокоуровневой архитектуре.
- Канонический источник процента выполнения: `memory_bank/projectbrief.md`.
- В `memory_bank/projectbrief.md` обязателен раздел `## Project Deliverables`.
- Создать раздел автоматически, если он отсутствует, при любой задаче, связанной с прогрессом, планированием или синхронизацией memory_bank.
- `## Project Deliverables` — канонический список результатов, по которому считается процент выполнения. Не журнал сессии.
- Каждый deliverable обязан иметь: стабильный ID, краткое название, статус (`pending` / `in_progress` / `completed` / `blocked`), вес в процентах.
- Сумма весов всех deliverables = `100`.
- Процент выполнения считается только по `## Project Deliverables`.
- `activeContext.md`, `progress.md`, `Changelog` — операционный контекст и история, не источник процента.
- Каждый новый пункт ТЗ или scope change добавлять в `## Project Deliverables` до или одновременно с реализацией.
- После каждой задачи, влияющей на готовность — обновить статусы deliverables.
- Если deliverables не синхронизированы с кодом — сначала восстановить, потом считать процент. Иначе процент = `нет достоверных данных`.

### 2. Память проекта

- Перед началом любой работы перечитай `/memory_bank`, особенно `activeContext.md` и `progress.md`.
- `/memory_bank` — твоя операционная система. Без актуализации теряешь контекст.
- Любые изменения в архитектуре, маршрутах или модулях отражать в `docs/README.md`.

### 3. Контроль изменений

- `progress.md` всегда содержит `last_checked_commit` (хеш или дата).
- При каждом сеансе сравнивай: `git log <last_checked_commit>...`
- При изменении документации — обновить хеш.

### 4. Структура Memory Bank

Обязательные файлы в `/memory_bank`:

| Файл | Назначение |
|------|-----------|
| `projectbrief.md` | Цели, рамки проекта, Project Deliverables |
| `productContext.md` | Продуктовый контекст, проблемы пользователей |
| `activeContext.md` | Текущий фокус, активные решения и приоритеты |
| `systemPatterns.md` | Архитектура, паттерны, связи подсистем |
| `techContext.md` | Стек, окружение, библиотеки, CI/CD |
| `progress.md` | Статус, Known Issues, Changelog, last_checked_commit |

Дополнительно: `modules/*`, `ui_extension/*`, `other/*`

### 5. Рабочие режимы

**Режим А — Планирование** (крупные/архитектурные/рискованные задачи):
1. Прочитай `productContext.md` и `activeContext.md`.
2. Составь план: шаги, файлы, риски.
3. Не приступай без одобрения пользователя.

Для повседневных правок и точечных фиксов — выполнять сразу.

**Режим Б — Выполнение:**
1. Обнови `activeContext.md` — задача взята в работу.
2. Выполняй последовательно.
3. Обновляй память: CI/CD → `techContext.md`; новый функционал → `docs/README.md`; legacy → `other/` + `progress.md`.
4. Обнови `progress.md` (Changelog) и `last_checked_commit`.

**Режим В — Инициализация** (Memory Bank пуст):
1. Создай файлы-заглушки.
2. Составь карту директорий и точек входа.
3. Запиши в `techContext.md` и `systemPatterns.md`.

### 6. Чек-лист перед завершением сессии

1. Проверил `git status` на неожиданные изменения?
2. Обновил `progress.md` и `activeContext.md`?
3. Синхронизировал `docs/README.md` при изменении архитектуры?
4. Указал новый `last_checked_commit`?

---

## План разработки проекта: BankBot + BridgeBot

### Обзор проекта

Проект состоит из трёх независимых ботов:

1. **BankBot** — Telegram-бот на **pytelegrambotapi** (синхронный), реализующий банковскую систему с балансами, начислением очков и покупками внутри чата. Требует комплексного рефакторинга: устранения уязвимостей безопасности, улучшения архитектуры, повышения надёжности и покрытия тестами. **Приоритет: ВЫСОКИЙ — реализуется после BridgeBot.**

2. **BridgeBot** — Telegram-бот на **pytelegrambotapi** (синхронный), который пересылает все посты из одного Telegram-канала в канал сообщества VK. **Приоритет: КРИТИЧЕСКИЙ — реализуется первым.**

3. **VK Bot** — бот в сообществе VK (на основе `vk_api`), который принимает сообщения от BridgeBot и публикует их в канале VK. **Приоритет: КРИТИЧЕСКИЙ — реализуется одновременно с BridgeBot.**

Все три компонента работают независимо, но используют общие библиотеки и конфигурацию (переменные окружения). Для взаимодействия BridgeBot отправляет сообщения через VK API (или через VK Bot) в канал VK.

**Схема взаимодействия:**
```
Telegram-канал (ID канала) → BridgeBot (telegram bot) → VK API → VK Bot (сообщество) → VK-канал
```

- BridgeBot подписывается на обновления канала Telegram (через `getUpdates` или вебхуки) и при появлении нового поста отправляет его текст и медиа в VK API.
- VK Bot (или напрямую токен сообщества) публикует сообщение в канале VK.

### Архитектура проекта

```text
┌─────────────────────────────────────────────────────────────┐
│                     Telegram                                │
│  ┌───────────────────┐      ┌──────────────────────────┐   │
│  │ Telegram-канал    │ ──►  │ BridgeBot (pytelegrambot)│   │
│  │ (исходный канал)  │      │ (обработка постов)       │   │
│  └───────────────────┘      └────────────┬─────────────┘   │
│                                           │                  │
│  ┌───────────────────┐      ┌────────────▼─────────────┐   │
│  │ BankBot           │      │ (опционально)            │   │
│  │ (банковская       │      │ взаимодействие           │   │
│  │ система)          │      │ через общую БД           │   │
│  └───────────────────┘      └──────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                     VK                                      │
│  ┌───────────────────┐      ┌──────────────────────────┐   │
│  │ VK API            │ ──►  │ VK Bot (сообщество)      │   │
│  │ (публикация)      │      │ (принимает и публикует)  │   │
│  └───────────────────┘      └────────────┬─────────────┘   │
│                                           │                  │
│                                    ┌──────▼──────┐           │
│                                    │ VK-канал   │           │
│                                    └─────────────┘           │
└─────────────────────────────────────────────────────────────┘
```

**Структура модулей (общая):**

```
BankBot/                     # корень репозитория
├── bridge_bot/              # BridgeBot (пересылка постов)
│   ├── main.py              # точка входа BridgeBot
│   ├── bot.py               # инициализация Telegram-бота (pytelegrambotapi)
│   ├── handlers.py          # обработчики (посты из канала)
│   ├── vk_publisher.py      # отправка в VK API
│   ├── media.py             # работа с медиафайлами (скачивание, загрузка)
│   ├── queue.py             # очередь сообщений и rate limiting
│   └── config.py            # конфигурация (переменные окружения)
├── bank_bot/                # BankBot (банковская система)
│   ├── main.py              # точка входа BankBot
│   ├── bot.py               # инициализация Telegram-бота (pytelegrambotapi)
│   ├── handlers/            # команды: balance, admin, shop
│   ├── services/            # бизнес-логика
│   ├── repositories/        # доступ к БД
│   ├── middleware.py        # обработка ошибок
│   └── di.py                # DI-контейнер
├── vk_bot/                  # VK Bot (публикация в канал)
│   ├── main.py              # точка входа VK Bot
│   ├── bot.py               # инициализация VK Long Poll
│   ├── handlers.py          # обработка входящих сообщений от BridgeBot
│   └── config.py            # конфигурация
├── common/                  # общие модули
│   ├── config.py            # общая конфигурация (Pydantic Settings)
│   ├── database.py          # единая БД (SQLAlchemy)
│   ├── models.py            # общие модели (пользователи, транзакции)
│   ├── logging.py           # логирование с ротацией
│   └── utils.py             # утилиты
├── tests/
│   ├── unit/
│   ├── integration/
│   └── e2e/
├── Dockerfile
├── docker-compose.yml
├── requirements.txt
├── run_bot.py - файл, запускающий ботов
```

### Требования (кратко)

Полные требования описаны в `requirements.md`. Ниже приведены ключевые группы с указанием приоритетов.

**Общие требования (О-1 – О-5)**
- Управление конфиденциальными данными (переменные окружения)
- Управление зависимостями (фиксированные версии, разделение prod/dev)
- Корректность импортов
- Документация проекта (README, DEPLOYMENT, диаграммы)
- Docstrings в публичных методах (Google style)

**BridgeBot и VK Bot (Б-1 – Б-13)**
- Конфигурация (Б-1, критический)
- Пересылка текста и медиа из Telegram-канала в VK-канал (Б-2, Б-3, Б-5, Б-6, критический)
- Фильтрация циклов (Б-4, критический) – если бот не будет пересылать свои же сообщения
- Очередь и rate limiting (Б-7, Б-8, высокий)
- Сохранение состояния (Б-9, высокий)
- Тестирование (Б-10, средний)
- Деплой и контейнеризация (Б-11, средний)
- Вебхуки (Б-12, низкий)
- Логирование (Б-13, высокий)

**BankBot (BB-1 – BB-18)**
- Централизованная конфигурация (BB-1, высокий)
- Слой репозиториев (BB-2, высокий)
- Service Layer (BB-3, высокий)
- Модульная структура (BB-4, высокий)
- Централизованная обработка ошибок (BB-5, высокий)
- Graceful Shutdown (BB-6, высокий)
- ParserRegistry в БД (BB-7, средний)
- Атомарность операций с балансом (BB-8, критический)
- Connection Pooling (BB-9, высокий)
- Защита от SQL-инъекций (BB-10, критический)
- Система алиасов (BB-11, средний)
- DI-контейнер (BB-12, высокий)
- Аудит неиспользуемого кода (BB-13, средний)
- Управление deprecated-кодом (BB-14, средний)
- Объединение дублирующихся парсеров (BB-15, средний)
- E2E тесты (BB-16, высокий)
- Тесты безопасности и надёжности (BB-17, высокий)
- Покрытие тестами 80%+ (BB-18, средний)

### Задачи реализации

Реализация ведётся в два этапа: сначала BridgeBot + VK Bot (критический приоритет), затем рефакторинг BankBot (высокий приоритет). Каждая фаза завершается чекпоинтом. Задачи, помеченные `*`, опциональны и могут быть пропущены для ускорения MVP.

#### 1. Инфраструктура и конфигурация (общая)
- [ ] 1.1 Создать `common/config.py` с Pydantic Settings
  - Поля: `BOT_TOKEN_BRIDGE`, `BOT_TOKEN_BANK`, `TG_CHANNEL_ID`, `VK_TOKEN`, `VK_PEER_ID`, `VK_GROUP_ID`, `APP_ENV`, `DATABASE_URL`, `ADMIN_CHAT_ID` и др.
  - Валидация: если токен или ID не заданы — ошибка.
- [ ] 1.2 Создать `requirements.txt` и `requirements-dev.txt`
  - Добавить `pytelegrambotapi~=4.x`, `vk_api~=11.x`, `sqlalchemy~=2.x`, `pydantic-settings~=2.x`
  - В dev: `pytest`, `pytest-cov`, `hypothesis`, `ruff`
- [ ] 1.3 Настроить общую БД (SQLAlchemy) с поддержкой SQLite/PostgreSQL
  - Создать миграции для таблиц: `users`, `balances`, `transactions`, `parser_registry`, `bridge_state`
- [ ] 1.4 Настроить логирование с ротацией

#### 2. BridgeBot + VK Bot (критический приоритет)
- [ ] 2.1 Создать структуру `bridge_bot/` и `vk_bot/`
- [ ] 2.2 Реализовать `bridge_bot/handlers.py` — обработчик постов из канала Telegram
  - Фильтр по `chat_id` (канал)
  - Извлечение текста, медиа (фото, видео, документы)
  - Добавление метки `[BOT]` для предотвращения циклов
- [ ] 2.3 Реализовать `bridge_bot/vk_publisher.py` — отправка в VK API
  - Методы: `send_text`, `send_photo`, `send_video`, `send_document`
  - Использовать токен сообщества VK
- [ ] 2.4 Реализовать `bridge_bot/media.py` — скачивание медиа из Telegram
- [ ] 2.5 Реализовать `bridge_bot/queue.py` — очередь сообщений и rate limiting
  - Воркер с задержкой между отправками
  - Обработка ошибок 429 (Retry-After)
- [ ] 2.6 Реализовать `vk_bot/main.py` — Long Poll для приёма сообщений от BridgeBot
  - Проверка метки `[BOT]`
  - Публикация в канал VK
- [ ] 2.7 Настроить graceful shutdown для обоих ботов
- [ ] 2.8 Написать unit-тесты для модулей BridgeBot и VK Bot
- [ ] 2.9 Написать интеграционные тесты (mock API)

**Чекпоинт — BridgeBot + VK Bot работают**
- Убедиться, что посты из Telegram-канала появляются в VK-канале.
- Проверить обработку медиа, rate limiting, восстановление после ошибок.

#### 3. Рефакторинг BankBot (высокий приоритет)
- [ ] 3.1 Создать слой репозиториев (`bank_bot/repositories/`)
  - `UserRepository`, `BalanceRepository`, `TransactionRepository`
  - Все запросы параметризованные
- [ ] 3.2 Создать Service Layer (`bank_bot/services/`)
  - `UserService`, `BalanceService`, `ShopService`
  - Бизнес-логика без вызовов pytelegrambotapi
- [ ] 3.3 Создать DI-контейнер (`bank_bot/di.py`)
- [ ] 3.4 Разбить монолитный код на модули команд (`balance`, `admin`, `shop`)
- [ ] 3.5 Реализовать Unit of Work для атомарных транзакций с балансом
- [ ] 3.6 Настроить Connection Pooling
- [ ] 3.7 Реализовать middleware для обработки ошибок с уведомлением администратора
- [ ] 3.8 Провести аудит SQL-запросов (параметризация)
- [ ] 3.9 Перенести парсеры в БД (ParserRegistry)
- [ ] 3.10 Объединить дублирующиеся парсеры
- [ ] 3.11 Реализовать систему алиасов
- [ ] 3.12 Провести аудит неиспользуемого кода (D&D, кланы)
- [ ] 3.13 Написать тесты: E2E, SQL-инъекции, race conditions, graceful shutdown
- [ ] 3.14 Настроить coverage (минимум 80%)

**Чекпоинт — BankBot отрефакторен**
- Все существующие команды работают.
- Тесты проходят, покрытие не ниже 80%.

#### 4. Деплой и документация
- [ ] 4.1 Написать `Dockerfile` и `docker-compose.yml` для всех трёх ботов
- [ ] 4.2 Добавить docstrings во все публичные методы (Google style)
- [ ] 4.3 Обновить `README.md` и создать `DEPLOYMENT.md`
- [ ] 4.4* Настроить вебхуки для Telegram-ботов (Flask + Nginx + SSL)

**Финальный чекпоинт**
- Убедиться, что все тесты проходят, линтер не выдаёт ошибок, боты запускаются в Docker.

### Ключевые риски и минимизация

#### BridgeBot + VK Bot
- **Блокировка API**: строго соблюдать лимиты, обрабатывать 429 с экспоненциальной задержкой.
- **Потеря сообщений**: сохранять `last_post_id` в БД, восстанавливать при перезапуске.
- **Циклы пересылки**: добавлять метку `[BOT]` ко всем пересылаемым сообщениям, фильтровать.
- **Утечка секретов**: хранить токены только в переменных окружения, `.env` в `.gitignore`.

#### BankBot
- **Потеря данных при конкурентных операциях**: Unit of Work с транзакциями и блокировками строк.
- **SQL-инъекции**: параметризованные запросы, статический анализ.
- **Race conditions**: тесты конкурентного изменения баланса, блокировки на уровне строк.
- **Непреднамеренное взаимодействие с BridgeBot**: общая БД может быть использована для хранения состояния, но бизнес-логика изолирована.

---

**Примечание:** Полные версии требований, технического дизайна и детальных задач находятся в файлах `requirements.md`, `design.md` и `tasks.md` соответственно. При работе над проектом обязательно обращайся к этим документам для уточнения деталей.
```